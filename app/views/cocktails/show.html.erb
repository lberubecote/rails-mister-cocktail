<h1><%= @cocktail.name %></h1>
  <ul>
    <p><% @cocktail.doses.each do |dose|%></p>
      <li><%= dose.description %> : <%= dose.ingredient.name %><%= link_to '- Delete a dose', dose_path(dose), method: :delete %> </li>
    <% end %>
  </ul>

<%= link_to "+ Add a new dose", new_cocktail_dose_path(@cocktail) %>
